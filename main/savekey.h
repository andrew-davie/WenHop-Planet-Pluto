#pragma once

enum CC {

    // ChronoColour modes.
    // Explicit values to prevent idiots rearranging the order.

    CC_NONE = 0,
    CC_PCC = 1,
    CC_ICC = 2,
};


extern enum CC saveKeyEnableICC;

// EOF
